# Deal Builder

Interactive Streamlit prototype for building produce deals with:

- supplier-by-commodity cards
- a `Build Deal` flow into `Deal Builder`
- contract-aware vs spot-market optimization
- an AI-style workspace chat that updates the live deal object
